<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bodymanagerapp">
    <!-- 만보기 사용을 위한 권한 설정 -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" /> <!-- 저장 권한 설정 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".menu.Diet.NewDietActivity"></activity>
        <activity android:name=".menu.Diet.DietActivity" />
        <activity android:name=".menu.Exercise.ExerciseAddtionActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>